# OS / Editor
.DS_Store
Thumbs.db
*.swp
*~

# Node / Tooling
node_modules/

# Build-Artefakte / Pakete
dist/
build/
*.zip

# Safari / Xcode (lokale Build-Ausgaben)
safari/xcode-wrapper/build/
*.xcuserstate
DerivedData/

# Routinen liegen unter scripts/ (versioniert). Build-/Docker-Kontext schliesst sie via .dockerignore aus.
